[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "hetman-kit-env"
authors = [{ name = "Hetman" }]
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["hetman-pipeline", "python-dotenv"]

[tool.setuptools.packages.find]
where = ["."]
